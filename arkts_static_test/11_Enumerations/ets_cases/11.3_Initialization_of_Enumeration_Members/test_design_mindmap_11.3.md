# 11.3 Initialization of Enumeration Members - 测试设计思维导图

## 概述
枚举成员可通过初始化器显式赋值；若基类为整数类型可省略初始化器。

## 规则
- 全部省略 → 第一成员 = 0，后续 = 前一 + 1
- 非整数基类必须显式初始化
- 部分显式 + 部分自动递增（Color: Red, Blue=5, Green → 0,5,6）
- 非 const 初始化器 → 后续成员必须有显式初始化器