# 11.4 Enumeration Methods - 测试设计思维导图

## 静态方法
- `values()` → 成员数组
- `getValueOf(name)` → 按名查找（不存在→运行时错误）
- `fromValue(value)` → 按值查找（不存在→运行时错误）

## 实例方法
- `toString()` → 基类值的字符串形式
- `valueOf()` → 基类原始值
- `getName()` → 成员名字符串

## 其余
- 索引：Color[color] → 成员名
- 同值 → textually last 优先