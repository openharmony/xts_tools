# 9.9.3 默认构造器 - 测试执行报告

**测试日期：** 2026-06-19
**编译器：** es2panda (ArkTS Static Compiler)
**运行时：** ark VM
**boot files：** arkstdlib.abc + etsstdlib.abc
**运行环境：** Linux (native)
**运行脚本：** `09_Classes/run_classes_cases_wsl.sh`

---

## 总体统计

| 分类 | 总数 | 通过 | 失败 | 通过率 |
|------|------|------|------|--------|
| compile-pass | 3 | 3 | 0 | 100% |
| compile-fail | 3 | 3 | 0 | 100% |
| runtime（真实执行） | 3 | 3 | 0 | 100% |
| **总计** | **9** | **9** | **0** | **100%** |

---

## 详细执行结果

### compile-pass（3/3 通过）

| # | 用例 ID | 测试内容 | 结果 |
|---|---------|---------|------|
| 001 | CLS_09_09_008 | 无显式构造器的类自动获得 public 隐式默认构造器，可正常实例化 | PASS |
| 002 | CLS_09_09_011 | 父类有可访问无参构造器时，无显式构造器的子类自动获得默认构造器 | PASS |
| 003 | CLS_09_09_012 | 无 extends 子句的类隐式继承 Object，自动获得 public 默认构造器 | PASS |

**说明：** 三个正向用例覆盖了默认构造器的基本场景——独立类、有父类的子类、隐式继承 Object 的类，均编译通过。

### compile-fail（3/3 通过）

| # | 用例 ID | 测试内容 | 结果 |
|---|---------|---------|------|
| 004 | CLS_09_09_009 | 父类仅有 private 构造器，子类默认构造器无法调用 super()，编译失败 | PASS |
| 005 | CLS_09_09_014 | 父类声明带参构造器后不再有无参构造器，子类默认构造器找不到可调用 super()，编译失败 | PASS |
| 006 | CLS_09_09_050 | 抽象父类仅含带参构造器，子类 extends 无显式构造器，编译失败 | PASS |

**说明：** 三个反向用例验证了默认构造器的约束——父类无 accessible 无参构造器时子类默认构造器必然编译失败，覆盖 private 构造器、仅有带参构造器、abstract 父类带参构造器等典型错误场景。

### runtime（3/3 — ark VM 真实执行 + assert）

| # | 用例 ID | 验证内容 | 断言数 | 结果 |
|---|---------|---------|--------|------|
| 007 | CLS_09_09_010 | 默认构造器正确执行字段初始化器（int、string） | 2 | PASS |
| 008 | CLS_09_09_013 | 三层继承默认构造器链式调用，字段初始化按父到子顺序正确执行 | 3 | PASS |
| 009 | CLS_09_09_015 | 继承层次中父子类多类型字段（int、string、boolean）初始化器全部正确执行 | 6 | PASS |

**说明：** 三个运行时用例在 ark VM 上真实执行并通过 assert 验证，覆盖字段初始化、多层继承链式调用、多类型字段初始化等场景，共 11 个断言全部通过。

---

## 后续运行

```bash
cd /home/nnd/projects/arkts/ARKTS_STATIC_TEST/09_Classes
SECTIONS="9.9.3_Default_Constructor" bash run_classes_cases_wsl.sh
```
