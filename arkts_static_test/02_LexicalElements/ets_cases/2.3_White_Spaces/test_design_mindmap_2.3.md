# 2.3 White Spaces - 测试设计思维导图（v2.1 - 增加测试因子checklist覆盖 + 补充cross_lang_verify目录）

## 概述
ArkTS 定义 6 种白空白符作为词法输入元素：
- Space (U+0020)
- Horizontal tabulation (U+0009)
- Vertical tabulation (U+000B)
- Form feed (U+000C)
- No-break space (U+00A0)
- Zero-width no-break space (U+FEFF)

White spaces 用于分隔 Token，被语法分析忽略，**不能出现在单个 Token 内部**，但**可以出现在注释中**。

> ⚠️ 重要说明：本测试设计参考 ArkTS Static Language Specification §2.3，并结合 TESTING_PROCESS_GUIDE.md 要求，增加测试因子checklist覆盖度。所有测试用例均已通过实际 ark VM 运行验证。

## 全覆盖测试矩阵

### 维度1：6种空白符独立测试

| 空白符 | Unicode | 名称 | 测试用例 |
|--------|---------|------|----------|
| Space | U+0020 | 空格 | 001 |
| Tab | U+0009 | 水平制表符 | 002 |
| VT | U+000B | 垂直制表符 | 003 |
| FF | U+000C | 换页符 | 004 |
| NBSP | U+00A0 | 不间断空格 | 005 |
| ZWNBSP | U+FEFF | 零宽不间断空格(BOM) | 006 |

### 维度2：白空白符使用场景

#### 2.1 正向场景（compile-pass）

##### A. 单个空白符分隔 Token
- **A1** Space 分隔 keyword 和 identifier
- **A2** Tab 分隔 keyword 和 identifier
- **A3** VT 分隔 keyword 和 identifier
- **A4** FF 分隔 keyword 和 identifier
- **A5** NBSP 分隔 keyword 和 identifier
- **A6** ZWNBSP 分隔 keyword 和 identifier

##### B. 多种空白符组合分隔 Token
- **B1** Space + Tab 混合
- **B2** 所有6种空白符混合
- **B3** 重复多个相同空白符

##### C. 白空白符在不同位置
- **C1** 在 keyword 之前（行首缩进）
- **C2** 在标识符之间
- **C3** 在运算符两侧
- **C4** 在分号之前
- **C5** 在大括号 `{` `}` 周围
- **C6** 在数组字面量元素间
- **C7** 在函数参数间

##### D. 白空白符在注释中（spec 显式允许）
- **D1** 单行注释内含 Tab
- **D2** 单行注释内含 NBSP
- **D3** 多行注释内含 VT、FF
- **D4** 多行注释内含 ZWNBSP

##### E. 白空白符在字符串/模板字符串中（属于 Token 内容，与分隔符无关）
- **E1** 字符串字面量内含 Tab/Space（合法字符串内容）
- **E2** 模板字符串内含各种空白

##### F. 行首缩进
- **F1** 仅 Space 缩进
- **F2** 仅 Tab 缩进
- **F3** Space + Tab 混合缩进

##### G. ZWNBSP / BOM 特殊场景
- **G1** 文件开头 BOM
- **G2** 文件中间 ZWNBSP 作为分隔符
- **G3** ZWNBSP 在多个 Token 之间

#### 2.2 反向场景（compile-fail）

##### H. 白空白符出现在 Token 内部
- **H1** 标识符中间含 Space
- **H2** 标识符中间含 Tab
- **H3** 数字字面量中含 Space
- **H4** 数字字面量中含 NBSP
- **H5** 关键字中含 Space（如 `cl ass`）
- **H6** 多字符运算符中含 Space（如 `= =`、`! ==`、`= >`）
- **H7** 字符字面量中 c'A' 之间含 Space

#### 2.3 运行时场景（runtime）

##### I. 各种空白风格不影响运行结果
- **I1** Space-only 风格运算结果
- **I2** Tab-indented 风格运算结果
- **I3** 6种混合空白风格运算结果
- **I4** 仅 NBSP 分隔的代码运算结果
- **I5** 行首缩进风格不影响语义
- **I6** 表达式内多空白不影响计算结果
- **I7** ZWNBSP 不影响字符串内容长度

## 用例编号规划（共约 38 个）

### compile-pass（21个，001~021）
- **维度1：6种空白符独立测试**
  - 001：Space 单独分隔 Token
  - 002：Tab 单独分隔 Token
  - 003：VT 单独分隔 Token
  - 004：FF 单独分隔 Token
  - 005：NBSP 单独分隔 Token
  - 006：ZWNBSP 单独分隔 Token
- **维度2：白空白符组合与位置场景**
  - 007：Space + Tab 混合分隔
  - 008：6种空白符全部混合
  - 009：多个相同空白符等价
  - 010：关键字前空白（行首缩进）
  - 011：标识符间的空白
  - 012：运算符两侧空白
  - 013：分号前空白
  - 014：大括号周围空白
  - 015：数组字面量元素间空白
  - 016：函数参数间空白
  - 017：单行注释内各种空白
  - 018：多行注释内各种空白
  - 019：字符串字面量内空白（内容）
  - 020：行首 Space + Tab 混合缩进
  - 021：ZWNBSP 在各种位置作分隔符

### compile-fail（10个，022~031）
- **维度3：白空白符在 Token 内部（禁止场景）**
  - 022：标识符中间含 Space（识别错误）
  - 023：标识符中间含 Tab（识别错误）
  - 024：数字字面量中含 Space（识别错误）
  - 025：数字字面量中含 NBSP（识别错误）
  - 026：关键字中含 Space（识别错误）
  - 027：`==` 拆分为 `= =`（识别错误）
  - 028：`&&` 拆分为 `& &`（识别错误）
  - 029：`=>` 拆分为 `= >`（识别错误）
  - 030：char字面量 `c'A'` 间含 Space（识别错误）
  - 031：字符串前缀与引号间含 Space

### runtime（7个，032~038）
- **维度4：运行时行为验证**
  - 032：Space-only 风格运算结果
  - 033：Tab-indented 风格运算结果
  - 034：6种空白混合风格运算结果
  - 035：NBSP 分隔风格运算结果
  - 036：不同缩进风格不影响语义
  - 037：表达式内多空白不影响计算
  - 038：ZWNBSP 不影响字符串内容长度

## 测试因子checklist 覆盖度分析

### ✅ 必须正交的因子（已覆盖）

| 因子分类 | 要求场景 | 2.3 覆盖情况 | 验证状态 |
|---------|---------|------------|---------|
| **基础语法位** | Token 内部包含空白符 | compile-fail (022~031) | ✅ 已验证 |
| | 空白符作为 Token 分隔符 | compile-pass (001~021) | ✅ 已验证 |
| | 空白符在注释中的合法性 | compile-pass (017~018) | ✅ 已验证 |
| | 空白符在字符串字面量中的内容属性 | compile-pass (019) + runtime (038) | ✅ 已验证 |

**评价：** ✅ **完全覆盖** - 所有基础语法位组合均已验证

---

### 🔍 需要重点关注的因子

#### 1. 白空白符在不同 Token 类型中的表现

| 场景 | 测试用例 | 验证状态 |
|------|---------|---------|
| 关键字与标识符之间的空白 | 001~006 | ✅ 已验证 |
| 标识符与冒号/类型注解之间 | 011 | ✅ 已验证 |
| 运算符两侧的空白灵活性 | 012 | ✅ 已验证 |
| 分号前后的空白 | 013 | ✅ 已验证 |
| 大括号 `{}` 周围的空白 | 014 | ✅ 已验证 |
| 数组字面量元素间的空白 | 015 | ✅ 已验证 |
| 函数参数间的空白 | 016 | ✅ 已验证 |
| 行首缩进风格（Space+Tab） | 020 | ✅ 已验证 |

**说明：** 这些场景在基础白空白符测试中全面覆盖，确保 6 种空白符在任何位置都能作合法分隔符。

---

#### 2. 特殊空白符 ZWNBSP (U+FEFF) 的特殊行为

| 场景 | 测试用例 | 规范要求 | 实际行为 | 验证状态 |
|------|---------|---------|---------|---------|
| BOM 标识文件 | 006 | 可作为分隔符 | ✅ 编译通过 | ✅ 已验证 |
| 多个 ZWNBSP 分隔 Token | 021 | 多个空白符等价 | ✅ 编译通过 | ✅ 已验证 |
| ZWNBSP 在字符串字面量中 | 038 | 字符串内容的一部分 | ✅ length 包含 | ✅ 已验证 |
| ZWNBSP 在注释中 | 017-018 | 允许在注释中 | ✅ 编译通过 | ✅ 已验证 |

**说明：** ZWNBSP 作为零宽字符，在运行时语义上与 Space 相同，但语义上被认为是"字符内容"。

---

#### 3. 编译器识别与解析机制

| 验证内容 | 测试用例 | 预期编译器行为 | 实际验证结果 |
|---------|---------|--------------|------------|
| 无效分割识别 | 022: Space in identifier | 报语法错误 | ✅ 报错 |
| 无效分割识别 | 024: Space in number | 报语法错误 | ✅ 报错 |
| 无效分割识别 | 027: `= =` 识别 | 报语法错误 | ✅ 报错 |
| 无效分割识别 | 030: `c 'A'` 识别 | 报语法错误 | ✅ 报错 |
| 多空白符规范化 | 009、037 | 多个相同空白符等价于一个 | ✅ 正常解析 |

**说明：** 测试验证了编译器对 6 种空白符的规范化处理机制，确保多个连续相同空白符不会产生不同的语法树。

---

## 分类说明
- **compile-pass**：合法空白符使用必须编译通过
- **compile-fail**：空白符在 Token 内部必须编译失败
- **runtime**：空白符变化不影响运行时语义，必须实际执行验证

## 文件命名规范
- 前缀：LEX_02_03 表示 Chapter 2 Section 3
- 示例：`LEX_02_03_001_PASS_SPACE_SEPARATOR.ets`
- 示例：`LEX_02_03_022_FAIL_SPACE_IN_IDENTIFIER.ets`

---

## 规范引用来源

### ArkTS Static Language Specification

1. **§2.3 White Spaces**
   - 语义：定义 6 种白空白符及其使用规则
   - 参考：spec/lexical.md 或相关规范章节

2. **§2.1 Lexical Tokenization**
   - 语义：空白符作为 Token 分隔符的词法分析规则
   - 参考：spec/lexical.md

3. **§3.5 String Literals**
   - 语义：字符串字面量内的字符（包括 ZWNBSP）作为内容的一部分
   - 参考：spec/expressions.md
