# 15.2.6 Subtyping for Function Types - 测试设计思维导图

## 测试范围

**规范章节**: 15.2.6 Subtyping for Function Types

**测试目标**: 验证 ArkTS 中函数类型的子类型关系，包括参数类型逆变、返回值类型协变等。

**关键概念**:
- 函数类型子类型: `(A) => B <: (C) => D` 当且仅当 `C <: A`（参数逆变）且 `B <: D`（返回值协变）
- 参数逆变: 参数类型必须反过来（父类参数可以接受子类参数）
- 返回值协变: 返回值类型必须一致或派生（子类返回值可以赋给父类变量）

## 测试用例矩阵

| 用例 ID | 类型 | 测试点 | 对应 Spec 章节 |
|---------|------|--------|----------------|
| SEM_15_02_006_PASS_FUNCTION_TYPE_SUBTYPE | compile-pass | 验证函数类型子类型：参数逆变，返回值协变 | 15.2.6 |
| SEM_15_02_017_FAIL_PARAM_COVARIANCE | compile-fail | 验证函数类型参数逆变违规拒绝：(Dog)=>void 不可用作 (Animal)=>void | 15.2.6 |
| SEM_15_02_06_100 | runtime | 函数类型子类型运行时 | 15.2.6 |

## 测试设计思路

### 1. 函数类型子类型（正向）
- **测试点**: 参数逆变 + 返回值协变
- **测试代码**: `(Animal) => Dog <: (Dog) => Animal`
- **预期结果**: 编译通过，运行时正常

### 2. 参数协变违规（反向）
- **测试点**: 参数协变违规拒绝
- **测试代码**: `(Dog) => void` 不可用作 `(Animal) => void`
- **预期结果**: 编译失败

### 3. 运行时行为（运行时）
- **测试点**: 验证函数类型子类型在运行时的行为
- **测试代码**: 传递函数作为参数
- **预期结果**: 运行时行为符合预期


## 最新设计要点

从章节思维导图同步的最新测试设计点：

- parameter contravariance
- return type covariance
- function type assignment compatibility

## 跨章节关联

- **15.2 Subtyping**: 子类型关系基础
- **15.5 Variance**: 类型方差（协变、逆变、不变）
- **3.24 Function Types**: 函数类型

## 测试环境

- **ArkTS/ArkUI-X**: DevEco Studio 5.0.3.510
- **Java**: Java 21.0.1
- **Swift**: Swift 5.9.2 + Xcode 15.2

## 注意事项

1. 函数类型的子类型关系是类型系统中最复杂的规则之一
2. 参数逆变是不符合预期的，需要详细解释
3. 运行时行为需要验证函数调用是否正常工作
