# 15.11.5 Overload Set for Class Instance Methods - 设计问题报告

---

## 报告分类口径

| 分类 | 含义 | 处理方式 |
|------|------|----------|
| 符合 ArkTS spec 的语言设计差异 | 行为与 Java/Swift 不同，但符合 ArkTS spec 或当前明确语义 | 不标为缺陷，仅记录差异 |
| Spec 与实现不一致 | 用例与 spec 要求不一致，且当前实现未按 spec 报错/运行 | 保留 FAIL 用例并记录 issue_report |
| 待确认问题 | 需要补充 stdlib/spec/实现依据后才能定性 | 暂不判定为缺陷 |
| 已验证规范一致行为 | 用例验证 ArkTS 行为符合 spec | 记录为通过项 |

**测试用例：** 3

---

## 一、行为差异与规范一致性概览

无重大问题。

---

## 二、已验证的 ArkTS 规范一致行为

- 类实例方法重载集：类中定义实例方法重载 ✅
- 通过实例调用实例方法：正确解析重载 ✅
- 类型不匹配拒绝：参数类型不匹配时编译报错 ✅
- 运行时验证：实例方法重载运行时正确调用 ✅

---

## 三、设计观察

### 观察：ArkTS 类实例方法重载与 Java 类实例方法重载一致

ArkTS 的类实例方法重载机制与 Java 的类实例方法重载机制类似，都是在类中定义多个同名实例方法形成重载集，通过实例调用实例方法时根据参数列表选择最匹配的版本。

### 观察：实例方法重载的调用方式

ArkTS 通过实例调用实例方法（`instance.method()`），这与 Java 的调用方式一致。

---

## 四、严重性

无问题。

---

## 五、改进建议

1. **补充更多测试用例**：当前只有3个测试用例，建议补充更多边界情况的测试，如继承中的实例方法重载、重写与重载的交互等。
2. **补充编译期拒绝测试**：当前缺少实例方法不存在的测试用例，建议补充。

---

## 六、跟踪记录

| 问题ID | 问题描述 | 状态 | 修复版本 |
|--------|----------|------|----------|
| - | - | - | - |

---

**报告生成时间**：2026-06-22
**报告状态**：✅ 无问题
