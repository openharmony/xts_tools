# 15.8.3 Difference Types（差分类型）- 测试设计思维导图

## 一、测试范围

验证 ArkTS 对差分类型（Difference Types）的支持行为，包括：
- 差分类型基本语法（`A & ~B`）
- 差分类型的子类型关系
- 差分类型的编译器支持状态（已知 GAP：ESY145527）
- 差分类型运行时行为

**注意**: 根据 Spec §15.8.3，ArkTS 定义了差分类型，但编译器暂不支持（已知 GAP，跟踪号 ESY145527）。差分类型依赖交叉类型，因此交叉类型未实现时，差分类型也无法实现。

## 二、测试用例矩阵

| 测试用例 ID | 类型 | 测试点 | 预期结果 |
|------------|------|--------|---------|
| SEM_15_08_03_001 | compile-pass | 类型自身赋值（简洁正向用例） | ✅ 通过 |
| SEM_15_08_03_100_FAIL_DIFFERENCE_UNSUPPORTED | compile-fail | 验证差分类型 — Spec §15.8.3 定义但编译器暂不支持（已知 GAP） | ⚠️ GAP |
| SEM_15_08_03_200 | runtime | 类型运行时验证 | ✅ 通过 |

## 三、跨章节关联

- **15.8 Smart Casts and Smart Types**（智能转换和智能类型）- 父章节
- **15.2.9 Subtyping for Difference Types**（差分类型的子类型关系）- 差分类型的子类型规则
- **15.8.2 Intersection Types**（交叉类型）- 差分类型依赖交叉类型

## 四、测试设计说明

### 4.1 正向测试用例
- **SEM_15_08_03_001**: 简洁的正向用例，声明 `string` 变量并赋值

### 4.2 反向测试用例
- **SEM_15_08_03_100_FAIL_DIFFERENCE_UNSUPPORTED**: 验证差分类型不支持（已知 GAP），Spec 定义但编译器未实现

### 4.3 运行时测试用例
- **SEM_15_08_03_200**: 验证类型运行时行为


## 最新设计要点

从章节思维导图同步的最新测试设计点：

- difference type computation in smart casts

## 五、覆盖率分析

| 规范条目 | 覆盖状态 | 备注 |
|---------|---------|------|
| 差分类型基本语法 | ⚠️ 已知 GAP | ESY145527，编译器未实现 |
| 差分类型的子类型关系 | ⚠️ 已知 GAP | 依赖编译器实现 |
| 差分类型运行时行为 | ⚠️ 已知 GAP | 依赖编译器实现 |

## 六、已知问题（GAP）

### 6.1 SEM_15_08_03_100_FAIL_DIFFERENCE_UNSUPPORTED

**问题描述**: Spec §15.8.3 定义了差分类型（`A & ~B`），但编译器暂不支持。

**Spec 要求**: 支持差分类型 `int & ~1`（表示从 `int` 中排除 `1`）。

**编译器行为**: 编译报错。

**分类**: 编译器实现待完善

**跟踪**: ESY145527

**依赖**: 差分类型依赖交叉类型，因此交叉类型未实现时，差分类型也无法实现。

**建议**: 编译器应实现差分类型支持，与 Spec 保持一致。
