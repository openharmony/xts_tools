# 15.7 Type Inference - 测试设计思维导图

## 测试范围
验证 ArkTS 类型推断（Type Inference）语义，包括：
- 基本类型推断：从初始化器推断类型（如 `let x = 42` 推断 x 为 int）
- 类型推断拒绝：类型不兼容时编译拒绝
- 运行时类型推断行为：推断类型在运行时正确
- 常量表达式类型推断（15.7.1）
- 返回类型推断（15.7.2）

## 测试用例矩阵

### compile-pass（编译通过）
| 用例 ID | 测试点 | 预期结果 |
|---------|--------|----------|
| SEM_15_07_100 | 类型推断：从初始化器推断类型 | compile-pass |

### compile-fail（编译失败）
| 用例 ID | 测试点 | 预期结果 |
|---------|--------|----------|
| SEM_15_07_099 | 类型推断拒绝：类型不兼容时编译拒绝 | compile-fail |

### runtime（运行时）
| 用例 ID | 测试点 | 预期结果 |
|---------|--------|----------|
| SEM_15_07_101 | 类型推断运行时行为：推断类型在运行时正确 | runtime |


## 最新设计要点

从章节思维导图同步的最新测试设计点：

- normal cases
- edge cases
- error cases

## 跨章节关联
- 15.7.1 Type Inference for Constant Expressions（常量表达式类型推断）
- 15.7.2 Return Type Inference（返回类型推断）
- 15.1 Semantic Essentials（语义基础，影响类型推断）

## 测试设计要点
1. **基本类型推断**：验证编译器能从初始化器推断变量类型
2. **类型推断拒绝**：验证类型不兼容时编译拒绝（如 `let x: int = "s"`）
3. **运行时验证**：验证推断类型在运行时行为正确
4. **常量表达式推断**：验证常量表达式的类型推断（子章节 15.7.1）
5. **返回类型推断**：验证函数返回类型的推断（子章节 15.7.2）

## 注意
- 本文件夹包含 15.7 的父级测试用例（3 个）
- 子章节测试用例在 15.7.1 和 15.7.2 文件夹中
- 需要分别修订父文件夹和子文件夹的 md 文件
