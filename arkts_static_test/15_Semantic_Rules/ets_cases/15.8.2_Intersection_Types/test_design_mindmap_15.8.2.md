# 15.8.2 Intersection Types（交叉类型）- 测试设计思维导图

## 一、测试范围

验证 ArkTS 对交叉类型（Intersection Types）的支持行为，包括：
- 交叉类型基本语法（`A & B`）
- 交叉类型的子类型关系
- 交叉类型的编译器支持状态（已知 GAP：ESY145527）
- 交叉类型运行时行为

**注意**: 根据 Spec §15.8.2，ArkTS 定义了交叉类型，但编译器暂不支持（已知 GAP，跟踪号 ESY145527）。

## 二、测试用例矩阵

| 测试用例 ID | 类型 | 测试点 | 预期结果 |
|------------|------|--------|---------|
| SEM_15_08_02_001 | compile-pass | 类型自身赋值（简洁正向用例） | ✅ 通过 |
| SEM_15_08_02_100_FAIL_INTERSECTION_UNSUPPORTED | compile-fail | 验证交叉类型 — Spec §15.8.2 定义但编译器暂不支持（已知 GAP） | ⚠️ GAP |
| SEM_15_08_02_200 | runtime | 类型运行时验证 | ✅ 通过 |

## 三、跨章节关联

- **15.8 Smart Casts and Smart Types**（智能转换和智能类型）- 父章节
- **15.2.8 Subtyping for Intersection Types**（交叉类型的子类型关系）- 交叉类型的子类型规则
- **4.4 Type Aliases**（类型别名）- 交叉类型可以使用类型别名定义

## 四、测试设计说明

### 4.1 正向测试用例
- **SEM_15_08_02_001**: 简洁的正向用例，声明 `int` 变量并赋值

### 4.2 反向测试用例
- **SEM_15_08_02_100_FAIL_INTERSECTION_UNSUPPORTED**: 验证交叉类型不支持（已知 GAP），Spec 定义但编译器未实现

### 4.3 运行时测试用例
- **SEM_15_08_02_200**: 验证类型运行时行为


## 最新设计要点

从章节思维导图同步的最新测试设计点：

- intersection type computation in smart casts

## 五、覆盖率分析

| 规范条目 | 覆盖状态 | 备注 |
|---------|---------|------|
| 交叉类型基本语法 | ⚠️ 已知 GAP | ESY145527，编译器未实现 |
| 交叉类型的子类型关系 | ⚠️ 已知 GAP | 依赖编译器实现 |
| 交叉类型运行时行为 | ⚠️ 已知 GAP | 依赖编译器实现 |

## 六、已知问题（GAP）

### 6.1 SEM_15_08_02_100_FAIL_INTERSECTION_UNSUPPORTED

**问题描述**: Spec §15.8.2 定义了交叉类型（`A & B`），但编译器暂不支持。

**Spec 要求**: 支持交叉类型 `Named & Aged`。

**编译器行为**: 编译报错。

**分类**: 编译器实现待完善

**跟踪**: ESY145527

**建议**: 编译器应实现交叉类型支持，与 Spec 保持一致。
