# 15.2.8 Subtyping for Intersection Types - 测试设计思维导图

## 测试范围

**规范章节**: 15.2.8 Subtyping for Intersection Types

**测试目标**: 验证 ArkTS 中交叉类型的子类型关系。⚠️ **注意：编译器暂不支持交叉类型（ESY145527）**，本测试主要用于概念验证和规范一致性检查。

**关键概念**:
- 交叉类型: `A & B` 表示同时满足 A 和 B 的类型
- 交叉类型子类型: `A & B <: A` 且 `A & B <: B`
- 交叉类型身份: `A & B` 可以赋值给 `A & B`

## 测试用例矩阵

| 用例 ID | 类型 | 测试点 | 对应 Spec 章节 | 状态 |
|---------|------|--------|----------------|------|
| SEM_15_02_08_001_PASS_PLACEHOLDER | compile-pass | 交叉类型—仅用于概念验证 | 15.2.8 | ⚠️ 编译器不支持 |
| SEM_15_02_009_FAIL_INTERSECTION_UNSUPPORTED | compile-fail | 验证交叉类型 — Spec 要求 A & B <: A 但编译器暂不支持交叉类型 | 15.2.8 | ⚠️ 编译器不支持 |
| SEM_15_02_08_100 | runtime | 交叉类型运行时 | 15.2.8 | ⚠️ 编译器不支持 |

## 测试设计思路

### 1. 交叉类型子类型（概念验证）
- **测试点**: `A & B <: A` 且 `A & B <: B`
- **测试代码**: `let x: A & B = ...; let a: A = x; let b: B = x;`
- **预期结果**: 规范定义，但编译器暂不支持（ESY145527）

### 2. 交叉类型身份（概念验证）
- **测试点**: `A & B` 可以赋值给 `A & B`
- **测试代码**: `let x: A & B = ...; let y: A & B = x;`
- **预期结果**: 规范定义，但编译器暂不支持（ESY145527）

### 3. 运行时行为（概念验证）
- **测试点**: 验证交叉类型运行时行为
- **测试代码**: 使用交叉类型进行赋值和函数调用
- **预期结果**: 规范定义，但编译器暂不支持（ESY145527）


## 最新设计要点

从章节思维导图同步的最新测试设计点：

- S<:T1&T2 when S<:T1 and S<:T2

## 跨章节关联

- **15.2 Subtyping**: 子类型关系基础
- **15.8 Smart Casts**: 智能类型转换（可能与交叉类型相关）
- **3.19 Union Types**: 联合类型（交叉类型的对偶）

## 测试环境

- **ArkTS/ArkUI-X**: DevEco Studio 5.0.3.510
- **Java**: Java 21.0.1
- **Swift**: Swift 5.9.2 + Xcode 15.2
- **TypeScript**: 5.3.3（参考）

## 注意事项

1. ⚠️ **编译器暂不支持交叉类型（ESY145527）**，测试用例可能无法通过编译
2. 交叉类型是 TypeScript 的特性，ArkTS 规范定义了但编译器未实现
3. 需要跟踪 ESY145527 的解决进度，并在编译器支持后更新测试用例

## 跟踪信息

- **编译器缺陷**: ESY145527
- **缺陷描述**: 编译器暂不支持交叉类型
- **缺陷状态**: 待修复
- **预计修复版本**: 待确认
