# 15.8.7 Smart Cast Examples（智能转换示例）- 测试设计思维导图

## 一、测试范围

验证 ArkTS 智能转换的典型使用场景，包括：
- `instanceof` 典型使用（`instanceof` 检查后类型收窄）
- 类型不匹配拒绝（类型不能赋值）
- 智能转换运行时行为

**注意**: 本节是 Reference section，提供智能转换的典型示例。

## 二、测试用例矩阵

| 测试用例 ID | 类型 | 测试点 | 预期结果 |
|------------|------|--------|---------|
| SEM_15_08_07_001 | compile-pass | 智能转换示例：典型 instanceof 使用（`instanceof` 检查后类型收窄） | ✅ 通过 |
| SEM_15_08_07_100 | compile-fail | 智能转换拒绝：类型不能赋值 | ✅ 通过 |
| SEM_15_08_07_200 | runtime | 智能转换运行时验证 | ✅ 通过 |

## 三、跨章节关联

- **15.8.1 Type Expression**（类型表达式）- 智能转换基于类型表达式
- **All 15.8 sub-sections**（所有 15.8 子章节）- 综合示例
- **4.15.4 Type Operators**（类型运算符）- `instanceof` 运算符

## 四、测试设计说明

### 4.1 正向测试用例
- **SEM_15_08_07_001**: 验证智能转换典型使用，`instanceof` 检查后在 `if` 分支中类型从 `Animal` 收窄为 `Dog`，可以访问 `Dog` 特有的成员

### 4.2 反向测试用例
- **SEM_15_08_07_200**: 验证智能转换拒绝，类型不能赋值（`int` 不能赋值给 `string`）

### 4.3 运行时测试用例
- **SEM_15_08_07_200**: 验证智能转换运行时行为


## 最新设计要点

从章节思维导图同步的最新测试设计点：

- union type smart cast
- type narrowing

## 五、覆盖率分析

| 规范条目 | 覆盖状态 | 备注 |
|---------|---------|------|
| instanceof 典型使用 | ✅ 已覆盖 | SEM_15_08_07_001 |
| 类型不匹配拒绝 | ✅ 已覆盖 | SEM_15_08_07_200 |
| 运行时行为 | ✅ 已覆盖 | SEM_15_08_07_200 |
