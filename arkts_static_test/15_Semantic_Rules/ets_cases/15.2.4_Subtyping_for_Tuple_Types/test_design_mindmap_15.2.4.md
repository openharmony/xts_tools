# 15.2.4 Subtyping for Tuple Types - 测试设计思维导图

## 测试范围

**规范章节**: 15.2.4 Subtyping for Tuple Types

**测试目标**: 验证 ArkTS 中元组类型的子类型关系，包括相同结构元组的赋值、元组元素类型的子类型关系等。

**关键概念**:
- 元组身份子类型: `[T1, T2]` 可以赋值给 `[T1, T2]`（相同结构）
- 元组协变: 如果 `T1 <: U1` 且 `T2 <: U2`，则 `[T1, T2] <: [U1, U2]`（如果支持协变）
- 元组长度: 元组长度必须匹配

## 测试用例矩阵

| 用例 ID | 类型 | 测试点 | 对应 Spec 章节 |
|---------|------|--------|----------------|
| SEM_15_02_008_PASS_TUPLE_IDENTITY | compile-pass | 验证元组类型的子类型关系：相同结构元组 <: 自身 | 15.2.4 |
| SEM_15_02_04_099 | compile-fail | 元组元素类型不匹配 | 15.2.4 |
| SEM_15_02_04_100 | runtime | 元组子类型运行时 | 15.2.4 |

## 测试设计思路

### 1. 元组身份子类型（正向）
- **测试点**: 相同结构元组可以赋值给相同结构的元组变量
- **测试代码**: `let t1: [int, string] = [1, "a"]; let t2: [int, string] = t1;`
- **预期结果**: 编译通过，运行时正常

### 2. 元组元素类型不匹配（反向）
- **测试点**: 元组元素类型不匹配时，不能赋值
- **测试代码**: `let t1: [int, string] = [1, "a"]; let t2: [string, int] = t1; // 应报错`
- **预期结果**: 编译失败

### 3. 运行时行为（运行时）
- **测试点**: 验证元组子类型在运行时的行为
- **测试代码**: 创建元组，进行赋值和访问
- **预期结果**: 运行时行为符合预期


## 最新设计要点

从章节思维导图同步的最新测试设计点：

- identical tuple types
- different tuple types (no subtyping)

## 跨章节关联

- **15.2 Subtyping**: 子类型关系基础
- **3.17.2 Tuple Types**: 元组类型
- **15.2.5 Subtyping for Union Types**: 联合类型的子类型关系

## 测试环境

- **ArkTS/ArkUI-X**: DevEco Studio 5.0.3.510
- **Java**: Java 21.0.1
- **Swift**: Swift 5.9.2 + Xcode 15.2

## 注意事项

1. ArkTS 的元组类型可能是可变长度的，需要确认规范
2. 元组协变/逆变的支持情况需要确认
3. 运行时行为需要验证元组的长度和元素类型是否保留
