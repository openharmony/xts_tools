# Prerequisite

```bash
sudo apt install pcregrep
```

# Usage

```bash
cd /path/to/dir/containing/projects    # e.g., /path/to/xts_acts
chmod +x /path/to/format_tc_doc.sh
/path/to/format_tc_doc.sh
```
