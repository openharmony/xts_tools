# Prerequisite

```bash
python3 -m venv .venv

# On Linux
source .venv/bin/activate

# Install Dependencies
pip install -r ./requirements.txt

# 将TestAbility.ets和Worker.ets模板置于resource目录下
```

# Usage

```bash
python3 -B ./main.py /path/to/xts_acts
```
