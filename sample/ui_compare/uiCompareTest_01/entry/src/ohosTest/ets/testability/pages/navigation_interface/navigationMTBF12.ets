/**
 * Copyright (c) 2025 Shenzhen Kaihong Digital Industry Development Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/**
 * NavigationMTBF12
 * 
 * 测试用例：SUB_ACE_UI_COMPONENT_NAV_NAVIGATION_MTBF_0120
 * 测试目的：Navigation组件页面跳转测试（pushDestinationByName + popToIndex场景）
 */

@Entry
@Component
struct NavigationMTBF12 {
  private pathStack: NavPathStack = new NavPathStack();

  build() {
    Navigation(this.pathStack) {
      Column() {
        Text('页面切换')
          .fontSize(20)
          .fontWeight(FontWeight.Bold)
          .margin({ bottom: 20 });

        Button('pushDestinationByName pageOne')
          .id('pushDestinationByNamePageOneBtn')
          .width(300)
          .height(50)
          .margin({ bottom: 10 })
          .onClick(() => {
            try {
              this.pathStack.pushDestinationByName('pageOne', null);
            } catch (e) {
              console.error('pushDestinationByName pageOne error:', e);
            }
          });

        Button('pushDestinationByName pageTwo')
          .id('pushDestinationByNamePageTwoBtn')
          .width(300)
          .height(50)
          .margin({ bottom: 10 })
          .onClick(() => {
            try {
              this.pathStack.pushDestinationByName('pageTwo', null);
            } catch (e) {
              console.error('pushDestinationByName pageTwo error:', e);
            }
          });
      }
      .width('100%')
      .height('100%')
      .padding(20)
      .backgroundColor('#FFFFFF')
    }
    .title('Navigation MTBF Test')
    .navDestination(PageOneBuilder)
    .navDestination(PageTwoBuilder)
  }
}

@Builder
export function PageOneBuilder(name: string, param: Object) {
  PageOne()
}

@Builder
export function PageTwoBuilder(name: string, param: Object) {
  PageTwo()
}

@Component
struct PageOne {
  private pathStack: NavPathStack | null = null;

  build() {
    NavDestination() {
      Column() {
        Text('PageOne')
          .fontSize(24)
          .fontWeight(FontWeight.Bold)
          .margin({ bottom: 20 });
      }
      .width('100%')
      .height('100%')
      .padding(20)
      .backgroundColor('#F5F5F5')
    }
    .title('PageOne')
    .onReady((context: NavDestinationContext) => {
      this.pathStack = context.pathStack;
    })
  }
}

@Component
struct PageTwo {
  private pathStack: NavPathStack | null = null;

  build() {
    NavDestination() {
      Column() {
        Text('PageTwo')
          .fontSize(24)
          .fontWeight(FontWeight.Bold)
          .margin({ bottom: 20 });

        Button('popToIndex 0')
          .id('popToIndex0Btn')
          .width(200)
          .height(50)
          .margin({ bottom: 10 })
          .onClick(() => {
            try {
              this.pathStack?.popToIndex(0);
            } catch (e) {
              console.error('popToIndex 0 error:', e);
            }
          });
      }
      .width('100%')
      .height('100%')
      .padding(20)
      .backgroundColor('#E5E5E5')
    }
    .title('PageTwo')
    .onReady((context: NavDestinationContext) => {
      this.pathStack = context.pathStack;
    })
  }
}

