/**
 * Copyright (c) 2025 Shenzhen Kaihong Digital Industry Development Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

@Entry
@Component
struct listScrollIndex08 {
  @State listData: string[] = [];
  @State firstIndex: number = -1;
  @State lastIndex: number = -1;
  @State centerIndex: number = -1;
  @State eventLog: string = '等待事件触发...';
  scrollerForList: Scroller = new Scroller();

  aboutToAppear() {
    // 初始化列表数据
    for (let i = 0; i < 100; i++) {
      this.listData.push(`Item ${i}`);
    }
  }

  addEvent(event: string) {
    this.eventLog = `${this.eventLog}\n${event}`;
    console.info('TEST', event);
  }

  build() {
    Column() {
      Text('用例编号：SUB_ACE_UI_COMPONENT_LIST_0210')
        .fontSize(14)
        .fontWeight(FontWeight.Bold)
        .margin({ bottom: 5 });
      Text('翻页滑动显示索引号正确')
        .fontSize(12)
        .margin({ bottom: 10 });

      Text(`当前可见索引: first=${this.firstIndex}, last=${this.lastIndex}, center=${this.centerIndex}`)
        .fontSize(12)
        .fontColor(Color.Blue)
        .margin({ bottom: 5 });

      Button('翻页滑动')
        .id('scrollPageButton_08')
        .onClick(() => {
          // 翻页滑动（向下翻一页）
          this.scrollerForList.scrollPage({ next: true });
          this.addEvent('翻页滑动 - 下一页');
        })
        .margin({ bottom: 10 });

      List({ space: 10, scroller: this.scrollerForList }) {
        ForEach(this.listData, (item: string, index: number) => {
          ListItem() {
            Text(`${index}: ${item}`)
              .id(`listItem_08_${index}`)
              .fontSize(16)
              .padding(10)
              .width('100%')
              .backgroundColor(index % 2 === 0 ? '#F0F0F0' : '#FFFFFF')
          }
          .id(`listItemContainer_08_${index}`);
        }, (item: string, index: number) => `${index}_${item}`);
      }
      .id('scrollIndexList_08')
      .listDirection(Axis.Vertical)
      .height(400)
      .width('100%')
      .onScrollIndex((firstIndex: number, lastIndex: number, centerIndex: number) => {
        this.firstIndex = firstIndex;
        this.lastIndex = lastIndex;
        this.centerIndex = centerIndex;
        this.addEvent(`onScrollIndex: first=${firstIndex}, last=${lastIndex}, center=${centerIndex}`);
      });

      Divider()
        .margin({ top: 10, bottom: 10 });

      Text('事件日志：')
        .fontSize(14)
        .fontWeight(FontWeight.Bold)
        .margin({ bottom: 5 });
      Scroll() {
        Text(this.eventLog)
          .fontSize(12)
          .width('100%')
          .textAlign(TextAlign.Start)
          .fontColor('#333333');
      }
      .width('100%')
      .height(150)
      .backgroundColor('#F5F5F5')
      .padding(5)
      .borderRadius(4)
      .scrollable(ScrollDirection.Vertical)
      .scrollBar(BarState.Auto);
    }
    .width('100%')
    .height('100%')
    .padding(20)
    .backgroundColor(0xF5F5F5)
  }
}

